/*
 * IncrementDecrease Messages
 *
 * This contains all the text for the IncrementDecrease component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.IncrementDecrease';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the IncrementDecrease component!',
  },
});
