/*
 * UserCacheBalance Messages
 *
 * This contains all the text for the UserCacheBalance component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.UserCacheBalance';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the UserCacheBalance component!',
  },
});
