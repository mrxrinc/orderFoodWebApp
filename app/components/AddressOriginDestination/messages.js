/*
 * AddressOriginDestination Messages
 *
 * This contains all the text for the AddressOriginDestination component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.AddressOriginDestination';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AddressOriginDestination component!',
  },
});
