/*
 * StickyPrice Messages
 *
 * This contains all the text for the StickyPrice component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.StickyPrice';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the StickyPrice component!',
  },
});
