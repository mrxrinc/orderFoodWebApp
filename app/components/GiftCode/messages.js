/*
 * GiftCode Messages
 *
 * This contains all the text for the GiftCode component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.GiftCode';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the GiftCode component!',
  },
});
