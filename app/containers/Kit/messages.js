/*
 * Kit Messages
 *
 * This contains all the text for the Kit container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.Kit';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Kit container!',
  },
});
