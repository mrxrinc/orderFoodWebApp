/*
 * AfterPayment Messages
 *
 * This contains all the text for the AfterPayment container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AfterPayment';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AfterPayment container!',
  },
});
